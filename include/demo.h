// 头文件：只放「声明」，不放实现（这是 C++ 工程的最基本分工）
#pragma once
#include <vector>

// TODO 区：你自己要练的东西，声明写这里，实现写 src/demo.cpp
int demo_sum(const std::vector<int>& v);

// 例：顺序表阶段可以换成这些
// void seqlist_init(...);
// bool seqlist_insert(...);
// bool seqlist_erase(...);
